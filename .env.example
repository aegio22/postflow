# Database connection string
# Format: postgres://user:password@host:port/dbname?sslmode=disable
DATABASE_URL="postgres://postgres:postgres@localhost:5432/postflow?sslmode=disable"

# Base URL where the postflow server is reachable
# For local dev with Docker or a local binary:
BASE_URL="http://localhost:8080"

# AWS / S3 configuration
AWS_REGION="us-east-1"
S3_BUCKET="your-postflow-bucket-name"

# AWS credentials (EXAMPLE ONLY — do NOT commit real keys)
AWS_ACCESS_KEY_ID="YOUR_AWS_ACCESS_KEY_ID"
AWS_SECRET_ACCESS_KEY="YOUR_AWS_SECRET_ACCESS_KEY"